version: 2.0

workflows:
  version: 2
  test_dockerize_deploy:
    jobs:
     # - test
     # - dockerize:
     #     requires:
     #       - test
      - deploy
     #     requires:
     #       - dockerize


jobs:
  test:
    docker:
      - image: python:3.6-alpine
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: pip install -r requirements.txt
      - run:
          name: Test CoDeChan
          command: python tests.py
  dockerize:
    machine: true
    steps:
      - checkout
      - run: docker login -u $DOCKER_USER -p $DOCKER_PASS
      - run: docker build -t $DOCKER_USER/codechan:$CIRCLE_SHA1 -t $DOCKER_USER/codechan:latest .
      - run: docker push $DOCKER_USER/codechan:$CIRCLE_SHA1
      - run: docker push $DOCKER_USER/codechan:latest
  deploy:
    docker:
      - image: circleci/python:3.7
    steps:
      - add_ssh_keys:
          fingerprints: 
            - "bc:b2:8d:c5:3c:61:9e:44:93:37:a3:2b:cf:36:09:a2"
      - checkout
      - run:
          name: "Deploy over SSH"
          command: |
            scp ./docker-compose.yml ubuntu@35.233.86.108:/opt/codechan/docker-compose.yml
            ssh -oStrictHostKeyChecking=no ubuntu@35.233.86.108 cat ./opt/codechan/docker-compose.yml
